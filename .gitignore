# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Fresh build directory
_fresh/
# npm + other dependencies
node_modules/
vendor/
# WebAwesome files (copied during build)
static/webawesome/
# Cache database files (versioned)
cache-v*.db
cache-v*.db-shm
cache-v*.db-wal

