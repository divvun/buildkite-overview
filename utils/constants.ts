// Global application constants

// Auto-refresh intervals
export const AUTO_REFRESH_INTERVAL_SECONDS = 5 // Fast refresh when tab is focused
export const AUTO_REFRESH_INTERVAL_IDLE = 30 // Slower when tab is unfocused
